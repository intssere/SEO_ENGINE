import { readFileSync } from "node:fs"; import { describe,expect,it } from "vitest"; const s=readFileSync(new URL("./controlled-git-connector.ts",import.meta.url),"utf8"); describe("git connector static boundary",()=>it("contains no transport, shell, fs mutation, or credentials",()=>{for(const token of ["fetch(","axios","@octokit","child_process","exec(","spawn(","simple-git","process.env","writeFile","unlink(","drizzle","postgres"])expect(s).not.toContain(token);expect(s).toContain("networkEnabled:false");expect(s).toContain("gitExecutionEnabled:false");expect(s).toContain("directDefaultBranchWrite:false");}));
