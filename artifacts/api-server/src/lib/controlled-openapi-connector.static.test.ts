import assert from "node:assert/strict";import test from "node:test";import {readFile} from "node:fs/promises";test("UGP-4.2 remains transport, credential and generator free",async()=>{const s=await readFile(new URL("./controlled-openapi-connector.ts",import.meta.url),"utf8");for(const token of ["fetch(","axios","openapi-fetch","@hey-api","child_process","node:http","node:https","process.env","drizzle","postgres"])assert.equal(s.includes(token),false,"forbidden boundary: "+token);assert.equal(s.includes("networkEnabled:false"),true);assert.equal(s.includes("executionEnabled:false"),true);});