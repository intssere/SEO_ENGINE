export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "3rem", maxWidth: 960, margin: "0 auto" }}>
      <p style={{ textTransform: "uppercase", letterSpacing: "0.12em", opacity: 0.6 }}>SEO Engine</p>
      <h1>Application foundation is running.</h1>
      <p>
        Task #1 establishes the web and worker runtime only. SEO intelligence, site connections,
        autonomous actions, and public-site writes are not enabled yet.
      </p>
    </main>
  );
}
